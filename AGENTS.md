# AGENTS.md

这是一个 Windows 本地运行的科技期刊图件提取与 AI 校对工具。

## 核心目标

1. 从 `.doc/.docx` 正文中识别第一作者、文章标题、图号和图题。
2. 优先直接提取 Word 中真实图对象；失败时才回退到 Word→PDF 后的裁图流程。
3. 将作者原稿图与重制后的 PDF/JPG 按“第一作者 + 图号”配对。
4. AI 校对必须区分“版式变化”和“实质内容差异”。
5. 文字校对按字符级严格处理：少字、错字、数字、单位、上下标、公式差异都不能因为语义相近而判 PASS。

## 当前稳定基线

- 当前基线版本：v1.2
- 默认分支：`main`
- `main` 视为当前可用版本。

## 简化开发规则

- 普通修复和小功能：可以直接提交到 `main`，不必创建分支、PR 或 Issue。
- 只有大改、重构、可能破坏现有功能的修改，才使用分支/PR。
- 用户通常只需要描述问题并提供截图、样本或结果 ZIP；Agent 应自行定位代码，不要求用户指出文件或函数。
- 修改完成后，告诉用户“改了什么 + 需要在 Windows 本机验证什么”即可。

## 不能破坏的行为

- 作者名以正文作者栏为准，不信任 Word 文件名。
- 图号/图题识别必须避免把正文中的“如图 4 所示”“图 4 展示……”误当成图题。
- 图件提取优先级：Word 原始对象 > PDF 明确对象 > PDF/页面区域回退。
- `.doc` 允许先通过 Word 临时转成 `.docx` 再处理，但不能修改原稿。
- 纯样式变化默认忽略：字体、字号、线宽、间距、重新布局、黑白转换等。
- 实质差异必须报告：文字、数字、单位、公式、节点、箭头、连接关系、层级、流程顺序、图例、坐标、数据等。
- AI 结果只允许 `PASS / REVIEW / FAIL`；无法确定时应降级为 `REVIEW`，不能猜测 PASS。

## API 与安全

- 当前支持/计划支持：阿里云百炼 Qwen、Groq、OpenRouter。
- 不要把 API Key 写进 HTML、JS、源码或 GitHub。
- Key 应保存在 Windows 凭据管理器、环境变量或本机配置中。
- 不得提交 `.env`、日志、运行时任务目录、临时稿件、作者原稿或校对结果 ZIP。

## 验证要求

代码修改后至少做：

- Python 语法检查；
- 前端 JS 语法/基本加载检查；
- 不改变现有启动逻辑；
- 涉及 Word COM、真实 `.doc/.docx`、Office 渲染的功能，需要用户在 Windows + Microsoft Word 环境做最终验证。

历史上已经暴露过的典型回归点：

- 旧服务占端口导致新版本“串台”；
- Word 图2把上方正文一起裁进去；
- 图4提取成空白；
- 正文“图4展示……”误识别为图题；
- AI 因语义脑补漏掉明显缺字。

修改相关模块时应主动避免这些问题复发。
